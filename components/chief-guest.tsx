import eventData from "@/data/event.json"

export function ChiefGuestSection() {
  const cg = eventData.greetings?.chief_guest
  return (
    <section id="chief-guest" className="py-12 md:py-16 bg-black text-white">
      <div className="container mx-auto px-4 max-w-6xl">
        
        
      </div>
    </section>
  )
}
